apiVersion: v1
kind: Secret
metadata:
  name: juicefs-secret-01
  namespace: kube-system
type: Opaque
stringData:
  name: 
  metaurl: 
  storage: 
  bucket: 
  access-key: 
  secret-key: 
  # 设置 Mount Pod 时区，默认为 UTC。
  # envs: "{TZ: Asia/Shanghai}"
  # 如需在 Mount Pod 中创建文件系统，也可以将更多 juicefs format 参数填入 format-options。
  # format-options: trash-days=1,block-size=4096